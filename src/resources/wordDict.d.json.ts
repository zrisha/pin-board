// Declaration for the generated wordDict.json (see scripts/parse-cedict.ts),
// so tsc types it as a plain record instead of parsing the 6.5 MB literal.
declare const wordDict: Record<string, string[]>;
export default wordDict;
