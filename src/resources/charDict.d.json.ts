// Declaration for the generated charDict.json (see scripts/parse-cedict.ts),
// so tsc types it as a plain record instead of parsing the 660 KB literal.
declare const charDict: Record<string, string[]>;
export default charDict;
